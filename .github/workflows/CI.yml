name: 🎡 CI

on:
  push:
    paths:
      - '.github/workflows/_test-package.yml'
      - '**.nupkg'

jobs:
  test-choco-packages:
    strategy:
      fail-fast: false
      matrix:
        packages:
          [
            '7tt',
            'action',
            'betterdiscord',
            'blitz.gg',
            'borderlessminecraft',
            'croc',
            'dcforoffice',
            'jarfix',
            'leagueoflegendseune',
            'nvm.install',
            'nvm.portable',
            'onefetch',
            'portmaster',
            'tcno-acc-switcher',
            'tree-sitter',
            'windhawk',
            'valorant',
          ]
    runs-on: windows-latest
    steps:
      - name: 🔧 Test packages
        uses: kamack38/packages/.github/workflows/_test-package.yml@master
        with: ${{ matrix.packages }}
